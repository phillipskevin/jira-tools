const styles = `
    @media (prefers-color-scheme: dark) {
        body {
            color: white;
        }
    }

    loading-indicator div {
        margin-top: 10px;
    }

    a.issue-link:visited {
        color: darkgray;
    }
`;

export default styles;
