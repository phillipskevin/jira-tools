const styles = `
    @media (prefers-color-scheme: dark) {
        body {
            color: white;
        }
    }

    loading-indicator div {
        margin-top: 10px;
    }
`;

export default styles;
